Запуск проекта с помощью docker.
Для упрощения запуска необходимо воспользоваться вложенным скриптом docker_start.sh:
% ./docker_start.sh

Для того, чтобы завершить docker можно восспользоваться следующей командой:
% docker stop $(docker ps -q)